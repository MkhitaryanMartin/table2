import React from 'react';
import TableContainer from '../../containers/home/table-container';

const Home = () => {
    return (
        <div>
          <TableContainer/>
        </div>
    );
};

export default Home;